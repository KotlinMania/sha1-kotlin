# Rust callers — `sha1-kotlin`

> Generated by `scaffold/generate_caller_reports.py` from `workspace_dep_graph.json` (Cargo-aware dep graph). Do not hand-edit. Re-run after the graph is rebuilt.

## What this file is

For every Rust crate this repo owns (under `tmp/`), this file lists every **cross-repo** workspace caller: every `use` statement in another `*-kotlin` repo's Rust source that imports a symbol from one of this repo's crates. Symbols are grouped per-crate; codex-kotlin sites are surfaced first per symbol (codex is the priority consumer driving the port queue).

**Granularity:** import-level (each row is one `use` line). Call-site detail — where the imported name is actually invoked, implemented, pattern-matched, or used as a generic bound inside the caller body — requires a per-symbol grep across the listed caller files. The graph tells you exactly which files to grep.

**Cross-repo only.** Imports between crates inside this same repo are omitted — those are intra-repo refactoring concerns, not the cross-project porting concern this file exists to surface.

**Cargo-resolved.** Counts here honor `package = "..."` renames, `workspace = true` inheritance, and `[target.<cfg>.dependencies]`. Stdlib (`std`, `core`, `alloc`) and intra-crate sub-module references are excluded.

## Crates in this repo

| package | module | inbound use sites | distinct caller crates | distinct caller repos | codex sites |
|---|---|---:|---:|---:|---:|
| `sha1` | `sha1` | 15 | 8 | 5 | 9 |

## `sha1`

### Inbound symbols (3 distinct, 15 total `use` sites)

Sorted by codex demand, then total demand. The first symbols are the priority queue for porting `sha1`.

#### `sha1::Digest` — 7 site(s) (5 codex, 2 other, 6 caller crates)

- ✅ `codex-kotlin/codex-analytics` `src/reducer.rs:68` — `use sha1::Digest`
- ✅ `codex-kotlin/codex-core` `src/turn_diff_tracker.rs:375` — `use sha1::Digest`
- ✅ `codex-kotlin/codex-mcp` `src/codex_apps.rs:21` — `use sha1::Digest`
- ✅ `codex-kotlin/codex-mcp` `src/tools.rs:19` — `use sha1::Digest`
- ✅ `codex-kotlin/codex-utils-cache` `src/lib.rs:6` — `use sha1::Digest`
- `axum-kotlin/axum` `src/extract/ws.rs:107` — `use sha1::Digest`
- `tungstenite-kotlin/tungstenite` `src/handshake/mod.rs:15` — `use sha1::Digest`

#### `sha1::Sha1` — 7 site(s) (3 codex, 4 other, 6 caller crates)

- ✅ `codex-kotlin/codex-mcp` `src/codex_apps.rs:22` — `use sha1::Sha1`
- ✅ `codex-kotlin/codex-mcp` `src/tools.rs:20` — `use sha1::Sha1`
- ✅ `codex-kotlin/codex-utils-cache` `src/lib.rs:7` — `use sha1::Sha1`
- `axum-kotlin/axum` `src/extract/ws.rs:107` — `use sha1::Sha1`
- `tiny-http-kotlin/tiny_http` `examples/websockets.rs:44` — `use sha1::Sha1`
- `tungstenite-kotlin/tungstenite` `src/handshake/mod.rs:15` — `use sha1::Sha1`
- `zip-kotlin/zip` `src/aes.rs:12` — `use sha1::Sha1`

#### `sha1::digest::Output` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-core` `src/turn_diff_tracker.rs:10` — `use sha1::digest::Output`
